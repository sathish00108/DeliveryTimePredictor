# .env.example
# Copy this file to .env and fill in any required values.
# This project does not currently require any API keys or external secrets.
# If you extend the app with LLM or cloud APIs, add your keys here and
# load them with: import os; value = os.environ.get("KEY_NAME")

# Example (not used):
# OPENAI_API_KEY=your-key-here
